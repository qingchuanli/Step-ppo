# HotpotQA agent with local FAISS CPU search.
# Kept this filename for backward compatibility with existing scripts.
- name: hotpotqa_agent
  _target_: recipe.hotpotqa.hotpotqa_agent_flow.HotpotQAAgentFlow
  max_steps: 5
  max_parallel_calls: 4
  force_first_search: true

  embedding_model_name: BAAI/bge-large-en-v1.5
  embedding_devices: null